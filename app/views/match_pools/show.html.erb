<article class="">
  <%= render 'top' %>

  <% if @match_pool.bets_opened_at %>
    <%= form_for :bets, url: match_pool_bets_path(@match_pool) do |f| %>
      <%= f.button 'Editar Mis Apuestas' %>
    <% end %>
  <% end %>

  <%= render 'scoreboard', user: current_user if @scoreboard %>

  <%= render 'matches/all' %>

  <% if current_user.admin? && !@match_pool.bets_opened_at %>
  <%= link_to 'Abrir Apuestas', advance_match_pool_path(@match_pool),
    class: 'btn btn-danger',
    data: { confirm: 'Estás seguro?' } %>
  <% end %>

</article>
