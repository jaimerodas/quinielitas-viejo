<nav>
  <%= link_to @match_pool.next_action, advance_match_pool_path(@match_pool), data: { confirm: 'Estás seguro?' } %>
</nav>

<% if @match_pool.accepts_bets? %>
  <%= form_for :bets, url: match_pool_bets_path(@match_pool) do |f| %>
    <%= f.button 'Editar Mis Apuestas' %>
  <% end %>
<% end %>

<%= render 'matches/all' %>
